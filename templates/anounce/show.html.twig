{% extends 'base.html.twig' %}
{% block title %}Details Announce !
{% endblock %}

{% block body %}


	<div class="container my-5">

		<div class="container py-5 text-center" style="background-image: url({{ announce.coverImage }}); color:white; height:400px;">
    
      <h6>{{ announce.title }}</h6> <br>
      <p>{{ announce.rooms }} chambres à {{ announce.price}}</p> <br>
      <p>{{ announce.introduction }}</p>

    </div>
    <div class="container">
                <p>{{ announce.description }}</p>

    </div>
    <div class="container">
      <div class="row">
        <div class="col-md-8">Commentaires</div>
        <div class="col">Auteurs</div>
      </div>
    </div>

	</div>


{% endblock %}
